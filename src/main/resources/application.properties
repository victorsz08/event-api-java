spring.application.name=api
spring.datasource.url=jdbc:postgresql://eventstec-db.cix4ucu0aui7.us-east-1.rds.amazonaws.com/postgres
spring.datasource.username=postgres
spring.datasource.password=McCFEk)v>EgSbWaD1x1#vpSgRI!D

# Let Hibernate not auto-create schema; Flyway will manage migrations
spring.jpa.hibernate.ddl-auto=none

# Flyway settings
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

aws.region=us-east-1
aws.s3.bucket=amz-bucket-jota
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY_ID}
cloud.aws.credentials.secret-key=${AWS_SECRET_ACCESS_KEY}